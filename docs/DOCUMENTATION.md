# Documentation

This workshop teaches parallel change in a .NET API backed by SQL Server.

The repository is organised to make expand, migrate, and contract visible in code, in the database, and in the HTTP contract.
